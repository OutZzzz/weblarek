# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

В приложении используются две сущности, которые описывают данные, — товар и покупатель.  
Они описаны такими интерфейсами:
 - `IProduct` - Товар
 - `IBuyer` - Покупатель

Для учёта данных в приложении есть три класса:
 - `Catalog` - хранение товаров, которые можно купить в приложении;
 - `Cart` - хранение товаров, которые пользователь выбрал для покупки;
 - `BuyerData` - данные покупателя, которые тот должен указать при оформлении заказа.

#### Интерфейс IProduct

- `id: string` - уникальный идентификатор товара.  
- `description: string` - Описание товара.  
- `image: string` - Изображение товара.  
- `title: string` - Название товара.  
- `category: string` - Категория товра.  
- `price: number | null` - цена товара.  

#### Интерфейс IBuyer

- `payment: TPayment` - вариант оплаты товаров.  
- `email: string` - email покупателя.  
- `phone: string` - телефон покупателя.  
- `address: string` - адрес покупателя.

#### Класс Catalog

Создержит в себе данные о товарах которые можно купить в приложении, а так же методы для работы с этими данными.

Конструктор:  
- `catalogItems: IProduct[]` - хранит массив всех товаров.  
- `itemData: IProduct | null` - хранит товар, выбранный для подробного отображения.

Методы:  
- `setCatalogItems(arr: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода.  
- `getCatalogItems(): IProduct[]` - получение массива товаров из модели.  
- `getItemById(id: string): IProduct` - получение одного товара по его id.  
- `setItemData(item: IProduct): void` - сохранение товара для подробного отображения.  
- `getItemData(): IProduct` - получение товара для подробного отображения.


#### Класс Cart

Содержит в себе данные о товарах, которые пользователь выбрал для покупки, а так же методы для работы с ними.  

Конструктор:  
- `cartItems: IProduct[]` - хранит массив товаров, выбранных покупателем для покупки.

Методы:  
- `getCartItems(): IProduct[]` - получение массива товаров, которые находятся в корзине.  
- `addItem(item: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины.  
- `removeItem(item: IProduct): void` - удаление товара, полученного в параметре из массива корзины.  
- `clearCart(): void` - очистка корзины.
- `getAllPrices(): number` - получение стоимости всех товаров в корзине.  
- `checkItemExist(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода.  


#### Класс BuyerData

Используется для хранения и обработки данных о пользователе.

Конструктор:  
- `payment: TPayment` - вид оплаты.  
- `address: string` - адрес.  
- `phone: string` - телефон.  
- `email: string` - email.

Методы:  
- `savePayment(data: TPayment): void` - сохранение данных об оплате в модели, в параметр принимает одно из трех значений.  
- `saveAddress(data: string): void` - сохранение данных об адресе в модели.  
- `savePhone(data: string): void` - сохранение данных об телефоне в модели.  
- `saveEmail(data: string): void` - сохранение данных об email в модели.  
- `getAllData(): IBuyer` - получение всех данных покупателя.  
- `clearData(): void` - очистка данных покупателя.  
- `validateData(): ValidationErrors` - валидация данных. Поле является валидным, если оно не пустое. Метод проверяет валидность и предоставляет информацию в случае ошибки.  

### Слой коммуникации

За коммуникации отвечает класс Queryes

#### Класс Queryes

Класс выполняет запросы к серверу.  

Конструктор:   
- `api: Api;` - Объект класса Api который используется для связи с сервером.

Методы:  
- `async getQuery(): Promise<IProduct[]>` - метод делающий GET запрос и получающий массив данных.  
- `async postQuery(order: IOrder): Promise<OrderResponse>` - метод отправляющий POST запрос на сервер

### Слой представления

Для реализации слоя представлевления используются следующие классы и интерфейсы

#### Интерфейс IHeader:  
- `counter: number` - задает тип принимаемого значения в класс Header

#### Класс Header

Класс наследуется от абстрактного класса `Component`. 
Отвечает за отображение счетчика.

Поля класса:  
- `basketButton: HTMLButtonElement` - поле для элемента типа button в разметке  
- `counterElement: HTMLElement` - поле для элемента отображающего счетчик товаров в корзине  

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.
- `this.counterElement` - получает элемент счетчика в текущем контейнере
- `this.basketButton` - получает элемент типа button в текущем контейнере
- `this.basketButton.addEventListener` - добавляет слушателя на собитие 'click' при нажатии на корзину 

Сеттеры:
- `set counter(value: number)` - добавляет в разметку значение счетчика

#### Интерфейс IGallery:
- `catalog: HTMLElement[]` - задает тип принимаемого значения в класс Gallery

#### Класс Gallery

Класс наследуется от абстрактного класса `Component`.
Отвечает за отображение всех товаров на странице.

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.

Сеттеры:
- `set catalog(value: HTMLElement[])` - добавляет в разметку массив элементов.

#### Интерфейс IModal:
- `content: HTMLElement` - задает тип принимаемого значения в класс Modal

#### Класс Modal

Класс наследуется от абстрактного класса `Component`.
Отвечает за отображение модального окна.

Поля класса:
- `contentElement: HTMLElement` - поле для элемента в который будет вставлятся контент
- `modalButton: HTMLButtonElement` - поле для элемента типа button отвечающего за закрытие модального окна

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.
- `this.contentElement` - получает элемент контейнер для последующей вставки контента в текущем контейнере
- `this.modalButton` - получает элемент типа button в текущем контейнере
- `this.modalButton.addEventListener` - добавляет слушателя на событие клика для закрытия модального окна

Сеттеры:
- `set content(data: HTMLElement)` - добавляет в разметку переданный контент.

#### Тип TBasket

- `order: HTMLElement[];` - задает тип принимаемого значения order в класс Basket.
- `totalPrice: number;` - задает тип принимаемого значения totalPrice в класс Basket.
- `empty: boolean;` - задает тип принимаемого значения empty в класс Basket.

#### Класс Basket

Класс наследуется от абстрактного класса `Component`.
Отвечает за отображение товаров в корзине.

Поля класса:
- `orderListElement: HTMLElement` - поле для элемента отображения списка товаров в корзине.
- `totalPriceElement: HTMLElement` - поле для элемента отображающего суммарную стоимость товаров в корзине.
- `buttonElement: HTMLButtonElement` - поле для элемента типа button для перехода на форму оформления.

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.
- `this.orderListElement` - получает элемент контейнер для последующей вставки списка товаров в текущем контейнере.
- `this.totalPriceElement` - получает элемент отображающий сумму товаров в корзине.
- `this.buttonElement` - получает элемент типа button для привязки слушателя.

Сеттеры:
- `set order(value: HTMLElement[])` - добавляет в разметку товары добавленные в корзину.
- `set totalPrice(value: number)` - добавляет в разметку сумму товаров в корзине.
- `set empty(value: boolean)` - добавляет в разметку в button параметр disable

#### Абстрактный класс Card

Класс наследуется от абстрактного класса `Component` и расщиряет его функционал вынося обшие поля и методы для последующий классов.

Поля класса:
- `priceElement: HTMLElement;` - поле для элемента отображения цены товара.
- `titleElement: HTMLElement;` - поле для элемента отображения названия товара.

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.
- `this.priceElement` - получает элемент отображающий цену товара.
- `this.titleElement` - получает элемент отображающий названия товара.

Сеттеры:
- `set price(value: number | null)` - задает значение отображаемое в элементе отображающего цену товара
- `set title(value: string)` - задает значение отображаемое в элементе отображающего наименование товара.

#### Класс CardBasket

Класс наследуется от абстрактного класса `Card`. Используется для создания и обновления разметки карточки товара в корзине.

Поля класса:
- `indexElement: HTMLElement` - поле для элемента отображения номера в списке.
- `buttonElement: HTMLButtonElement` - поле для элемента типа button используюшегося для  удаления товара из корзины

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.
- `this.indexElement` - получает элемент отображающий номера в списке
- `this.buttonElement` - получает элемент типа button используюшегося для  удаления товаром из корзины
- `this.buttonElement.addEventListener('click', actions.onClick)` - конструкция позволяющая передавать данные в событийный триггер так чтобы класс представления ничего не знал о данных

Сеттеры:
- `set index(value: number)` - задает значение отображаемое в элементе который отображает номера в списке

#### Класс CardCatalog

Класс наследуется от абстрактного класса `Card`. Используется для создания и обновления разметки каталога товаров.

Поля класса:
- `imageElement: HTMLImageElement` - поле для элемента отображающиего изображения карточек
- `categoryElement: HTMLElement` - поле для элемента отображающего категорию

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.
- `this.categoryElement` - получает элемент отображающий категорию в карточке
- `this.imageElement` - получает элемент отображающий изображение в карточке
- `this.buttonElement.addEventListener('click', actions.onClick)` - конструкция позволяющая передавать данные в событийный триггер так чтобы класс представления ничего не знал о данных

Сеттеры:
- `set category(value: string)` - задает категорию отображающуюся в карточке
- `set image(value: string)` - задает изображение отображающееся в карточке

#### Класс CardPreview

Класс наследуется от абстрактного класса `Card`. Используется для создания и обновления разметки карточки превью товара.

Поля класса:
- `imageElement: HTMLImageElement` - поле для элемента отображающиего изображения карточек.
- `categoryElement: HTMLElement` - поле для элемента отображающего категорию.
- `buttonElement: HTMLButtonElement` - поле для элемента типа button отвечающего за добавление товара в корзину.
- `textElement: HTMLElement;` - поле для элемента отображающего отписание товара.

Конструктор:  
- `super(container)` - вызывает родительский параметр, отвечает за ограничение действия класса текущим контейнером переданным в конструкторе.
- `this.categoryElement` - получает элемент отображающий категорию в карточке
- `this.imageElement` - получает элемент отображающий изображение в карточке
- `this.buttonElement` - получает элемент типа button отвечающий за добавление товара в корзину
- `this.textElement` - получает элемент отображающий отписание товара.
- `this.buttonElement.addEventListener('click', actions.onClick)` - конструкция позволяющая передавать данные в событийный триггер так чтобы класс представления ничего не знал о данных

Сеттеры:
- `set description(value: string)` - задает значение элементу отображающему описание товара.
- `set category(value: string)` - задает значение элементу отображающему категорию
- `set button(value: {text: string; disabled: boolean})` - задает значение текста в кнопке и включает / выключает ее.

